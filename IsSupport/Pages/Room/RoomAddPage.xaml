<Page x:Class="IsSupport.Pages.Room.RoomAddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IsSupport.Pages.Room"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoomAddPage" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Укажите номер кабинета" Style="{StaticResource TextBlockDefault}"/>
        <Button Name="BtnAddRoom" Grid.Row="1" Content="Сохранить" Style="{StaticResource ButtonStatusCartridg}" Click="BtnAddRoom_Click"/>
        <TextBox Name="TbTitleRoom" Grid.Column="1" Text="{Binding Title}" Style="{StaticResource TextBoxDefault}"/>
        <Button Name="BtnCancel" Grid.Row="1" Grid.Column="1" Content="Отмена" Style="{StaticResource ButtonStatusCartridg}" Click="BtnCancel_Click"/>
    </Grid>
</Page>
