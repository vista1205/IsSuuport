<Page x:Class="IsSupport.Pages.MultimediaSystem.MultimediaSystemsAddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IsSupport.Pages.MultimediaSystem"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Мультимедиасистема: Добавление/Редактирование" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Укажите название мультимедиасистемы" Style="{DynamicResource MaterialDesignTextBlock}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Text="Выберите кабинет" Grid.Row="1" Style="{DynamicResource MaterialDesignTextBlock}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBox x:Name="TbTitleSystem" Grid.Row="0" Grid.Column="1" Text="{Binding Title}" Style="{DynamicResource MaterialDesignOutlinedTextBox}"/>
        <ComboBox Name="CmbRoomsList" DisplayMemberPath="Title" Grid.Row="1" Grid.Column="1" SelectedItem="{Binding Rooms}" Style="{DynamicResource MaterialDesignOutlinedComboBox}"/>
        <Button Name="BtnAddSystem" Content="Сохранить" Grid.Row="2" Click="BtnAddSystem_Click" Width="150"/>
        <Button Name="BtnCancel" Content="Отмена" Grid.Row="2" Grid.Column="1" Click="BtnCancel_Click" Width="150"/>
    </Grid>
</Page>
